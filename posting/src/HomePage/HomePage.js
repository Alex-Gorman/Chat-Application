import React from 'react';

function HomePage() {
    return (
        <div>
            <p>This chat application allows users to create channels,
                view all channels, select a channel and post messages
                to that channel, and post replies to existing messages.
            </p> 

        </div>
    );
}

export default HomePage;